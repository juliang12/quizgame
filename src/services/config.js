const NAME = "opentdb";
const DOMAIN = `https://${NAME}.com`;
const QUESTIONS = "api.php?amount=4&category=14";
const SITE = `${DOMAIN}/${QUESTIONS}`

export{
    NAME,
    DOMAIN,
    QUESTIONS,
    SITE
}